[project]
name = "boijelux-v7"
version = "7.1.0"
description = "Boijelux v7.1 - Complete API"
requires-python = ">=3.9"
dependencies = [
    "fastapi>=0.100.0",
    "uvicorn>=0.20.0",
]

[tool.vercel]
entrypoint = "api.index:handler"
