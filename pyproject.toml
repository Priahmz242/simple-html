[project]
name = "unlimited-ai-agent"
version = "2.0.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.6",
    "uvicorn[standard]>=0.34.0",
]

[tool.vercel]
entrypoint = "api.index:handler"
